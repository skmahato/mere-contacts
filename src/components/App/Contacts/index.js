import React, { Component, Fragment } from 'react';

import './index.scss';

class Contacts extends Component {
  render() {
    return (
      <Fragment>
        <h1>Contacts</h1>
      </Fragment>
    );
  }
}

export default Contacts;
